{
  "dependencies": {
    "benchmark": "^2.1.4",
    "esbuild": "^0.14.13",
    "microtime": "^3.0.0",
    "mustache": "^4.2.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "scripts": {
    "start": "npm run build && node ./dist.js",
    "build": "esbuild ./index.js --bundle --loader:.js=jsx --platform=node --outfile=dist.js"
  }
}
